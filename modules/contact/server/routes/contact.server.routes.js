'use strict';

/**
 * Module dependencies
 */
var // the contact controller to handle routes
    contactController = require('../controllers/contact.server.controller');

module.exports = function (app) {
    // GET contact page information
	// format /api/contact
    //app.route('/api/contact').get(contactController.read);
};