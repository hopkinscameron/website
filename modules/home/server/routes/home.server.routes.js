'use strict';

/**
 * Module dependencies
 */
var // the home controller to handle routes
    homeController = require('../controllers/home.server.controller');

module.exports = function (app) {
    // GET home page information
	// format /api/home
    //app.route('/api/home').get(homeController.read);
};