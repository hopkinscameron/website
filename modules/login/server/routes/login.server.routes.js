'use strict';

/**
 * Module dependencies
 */
var // the login controller to handle routes
    loginController = require('../controllers/login.server.controller');

module.exports = function (app) {
    // GET login page
	// format /login
    //app.route('/login').get(loginController.read);
};