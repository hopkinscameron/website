'use strict';

/**
 * Module dependencies
 */
var // the resume controller to handle routes
    resumeController = require('../controllers/resume.server.controller');

module.exports = function (app) {
    // GET resume page information
	// format /api/resume
    //app.route('/api/resume').get(resumeController.read);
};